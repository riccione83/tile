<%= form_for(@work, :html => {:multipart => true})  do |f| %>

  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <%= f.hidden_field :user_id, :value => current_user.id %>

  <div class="field">
    <%= f.label "Titolo descrittivo" %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "Descrizione dei lavori" %><br>
    <%= f.cktext_area :description %>
  </div>


  <div class="field">
    <%= f.label "Città" %><br>
    <%= f.collection_select :location, regioni, :to_s, :to_s, include_blank: false %>
  </div>
  
  
  <div class="field">
  <div class="control-group">
    <%= f.label "Inserisci le eventuali foto", class: 'control-label' %>
    <div class="controls">
      <%= file_field_tag "images[]", type: :file, multiple: true %>
    </div>
  </div>
  </div>
  <hr>
  <% if current_user.id == @work.user_id || @work.new_record? %>
    <div class="actions">
      <%= f.submit "Invia" %>
    </div>
  <% end %>
<% end %>
